import sys
import os
import ast

nq = sys.eval[1]
ps = ast.literlal_eval(sys.eval[2])
gammas = ast.literlal_eval(sys.eval[3])
betas = ast.literal_eval(sys.eval[4])
cases = ast.litelal_eval(sys.eval[5])

for p in ps:
	for case in cases:
		delta_beta = betas[p]
		delta_gamma = gammas[p]
		



